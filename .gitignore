# Rust
/target
**/*.rs.bk
Cargo.lock.bak

# OS junk
.DS_Store

# Runtime artefacts
/data
/logs
/out

# Secrets — config.toml ist nur lokal, Repo trägt nur *.sample.toml
/config/config.toml
/.env
/.env.*
!/.env.example

# Kunden-Samples (Testdata) bleiben lokal, nicht im Repo — enthalten
# reale Namen/Adressen aus den Lobster-Beispielausgaben.
/Testdata
